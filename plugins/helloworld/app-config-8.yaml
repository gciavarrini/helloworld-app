app:
  baseUrl: "this is a base url"
  myItems: ["itemA", "itemB"]
backend:
  baseUrl: "this is backend baseUrl"
  database:
    client: "pg"
    connection: "aloha connection"
  listen: "listen"
techdocs:
  randomPropertyTechDocs: "hello"


# » backstage-cli config:check --config ./app-config-8.yaml --strict
# Loaded config from app-config-8.yaml

# Error: Configuration does not match schema

#   Config must NOT have additional properties { additionalProperty=randomPropertyTechDocs } at /techdocs