app:
  baseUrl: "this is a base url"
  myItems: ["itemA", "itemB"]
backend:
  baseUrl: "this is backend baseUrl"
  database: "a db"
  listen: "listen"
techdocs: 123


# » backstage-cli config:check --config ./app-config-5.yaml --strict
# Loaded config from app-config-5.yaml

# Error: Configuration does not match schema

#   Config must be object { type=object } at /backend/database
#   Config must be object { type=object } at /techdocs
